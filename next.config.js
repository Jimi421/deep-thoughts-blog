/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    // you can re‐enable errors after fixing types
    ignoreBuildErrors: false,
  },
};

module.exports = nextConfig;
